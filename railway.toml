[build]
builder = "dockerfile"
 
[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300   # PaddleOCR takes a while to download models on cold start
restartPolicyType = "on_failure"